# Server Configuration
BIND_ADDRESS=0.0.0.0:8080

# Logging Configuration
# Levels: error, warn, info, debug, trace
RUST_LOG=info

# Optional: GStreamer Debug (if needed for troubleshooting)
# GST_DEBUG=3
